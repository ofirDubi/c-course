#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAXLENGTH 100

/* use shift to move every entering number in the char*/
typedef char set[MAXLENGTH];
void print_set(set S);
void read_set(char input[]);